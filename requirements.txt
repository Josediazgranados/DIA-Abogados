# --- Stack actual (arquitectura recomendada por firma-poderes/README.md,
#     sección "De prototipo a producción") ---
fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
python-multipart>=0.0.9
jinja2>=3.1
reportlab>=4.0
pillow>=10.0
requests>=2.31              # WhatsApp real vía Meta Cloud API (ver app/services/notify.py)
python-dotenv>=1.0          # carga automática de .env en desarrollo local

# --- Para verificar (demo.py usa TestClient de FastAPI) ---
httpx>=0.27

# --- Para producción con PostgreSQL (basta con exportar DATABASE_URL) ---
# Incluida siempre (no solo comentada): en hosting como Render el disco
# NO es persistente entre despliegues/reinicios, así que ahí SIEMPRE hace
# falta Postgres para no perder los datos — ver README, sección "Desplegar".
psycopg2-binary>=2.9        # o asyncpg si se usa SQLAlchemy async

# --- Pendiente: procesamiento asíncrono para picos de miles de invitaciones
#     y almacenamiento en la nube. Ver README.md, sección "Próximos pasos". ---
# celery>=5.3
# redis>=5.0
# boto3>=1.34                 # almacenamiento S3-compatible cifrado
